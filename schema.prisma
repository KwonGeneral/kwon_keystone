// This file is automatically generated by Keystone, do not modify it manually.
// Modify your Keystone config when you want to change this.

datasource mysql {
  url               = env("DATABASE_URL")
  shadowDatabaseUrl = env("SHADOW_DATABASE_URL")
  provider          = "mysql"
}

generator client {
  provider = "prisma-client-js"
}

model AppInfoModel {
  id                                         String                      @id @default(uuid())
  version                                    String                      @default("")
  name                                       String                      @default("")
  header                                     String                      @default("")
  title                                      String                      @default("")
  content                                    String                      @default("")
  description                                String                      @default("")
  buildNumber                                String                      @default("")
  packageName                                String                      @default("")
  updatedAt                                  DateTime?                   @default(now()) @updatedAt
  createdAt                                  DateTime?                   @default(now())
  from_WebSiteInfo_AppInfo_Model_appInfoList WebSiteInfo_AppInfo_Model[] @relation("WebSiteInfo_AppInfo_Model_appInfoList")
}

model AttachmentModel {
  id        String    @id @default(uuid())
  name      String    @default("")
  size      Int?
  base64    String    @default("")
  extension String    @default("")
  type      String    @default("")
  updatedAt DateTime? @default(now()) @updatedAt
  createdAt DateTime? @default(now())
}

model LocationModel {
  id            String    @id @default(uuid())
  name          String    @default("")
  address       String    @default("")
  addressDetail String    @default("")
  latitude      String    @default("")
  longitude     String    @default("")
  updatedAt     DateTime? @default(now()) @updatedAt
  createdAt     DateTime? @default(now())
}

model PostModel {
  id          String    @id @default(uuid())
  version     String    @default("")
  header      String    @default("")
  title       String    @default("")
  content     String    @default("")
  description String    @default("")
  author      String    @default("")
  status      String    @default("")
  url         String    @default("")
  isTopFixed  String    @default("")
  level       String    @default("")
  updatedAt   DateTime? @default(now()) @updatedAt
  createdAt   DateTime? @default(now())
}

model RuleModel {
  id          String    @id @default(uuid())
  name        String    @default("")
  header      String    @default("")
  title       String    @default("")
  content     String    @default("")
  description String    @default("")
  updatedAt   DateTime? @default(now()) @updatedAt
  createdAt   DateTime? @default(now())
}

model TermsModel {
  id          String    @id @default(uuid())
  name        String    @default("")
  header      String    @default("")
  title       String    @default("")
  content     String    @default("")
  description String    @default("")
  updatedAt   DateTime? @default(now()) @updatedAt
  createdAt   DateTime? @default(now())
}

model UserModel {
  id            String    @id @default(uuid())
  level         Int       @default(0)
  name          String    @default("")
  email         String    @unique @default("")
  password      String
  phone         String    @default("")
  address       String    @default("")
  addressDetail String    @default("")
  gender        String    @default("")
  birthDay      String    @default("")
  updatedAt     DateTime? @default(now()) @updatedAt
  createdAt     DateTime? @default(now())
}

model WebSiteInfoModel {
  id                                       String                      @id @default(uuid())
  url                                      String                      @default("")
  name                                     String                      @default("")
  header                                   String                      @default("")
  title                                    String                      @default("")
  content                                  String                      @default("")
  description                              String                      @default("")
  updatedAt                                DateTime?                   @default(now()) @updatedAt
  createdAt                                DateTime?                   @default(now())
  from_WebSiteInfo_AppInfo_Model_websiteId WebSiteInfo_AppInfo_Model[] @relation("WebSiteInfo_AppInfo_Model_websiteId")
}

model WebSiteInfo_AppInfo_Model {
  id          String            @id @default(uuid())
  websiteId   WebSiteInfoModel? @relation("WebSiteInfo_AppInfo_Model_websiteId", fields: [websiteIdId], references: [id])
  websiteIdId String?           @map("websiteId")
  appInfoList AppInfoModel[]    @relation("WebSiteInfo_AppInfo_Model_appInfoList")
  updatedAt   DateTime?         @default(now()) @updatedAt
  createdAt   DateTime?         @default(now())

  @@index([websiteIdId])
}
